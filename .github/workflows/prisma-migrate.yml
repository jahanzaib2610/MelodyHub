name: Run Prisma Migrations (deploy)

on:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  migrate:
    name: Apply Prisma Migrations
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma Client
        run: npx prisma generate

      - name: Run Prisma Migrate Deploy
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: npx prisma migrate deploy

      - name: (Optional) Run tests
        if: always()
        run: npm test || echo "No tests configured"
name: Run Prisma Migrations (deploy)

on:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  migrate:
    name: Apply Prisma Migrations
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma Client
        run: npx prisma generate

      - name: Run Prisma Migrate Deploy
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: npx prisma migrate deploy

      - name: (Optional) Run tests
        if: always()
        run: npm test || echo "No tests configured"

name: Resolve Prisma Migration (manual)
on:
  workflow_dispatch:

jobs:
  resolve-migration:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      - name: Resolve migration as applied
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: npx prisma migrate resolve --applied "20251017_init"
